name: Build PLC110 Ultra Optimized (LTO+FDO Fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 环境准备与工具链
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential flex gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu git git-lfs gnupg gperf libncurses5-dev libssl-dev python3 rsync zip curl ccache
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          echo "$HOME/bin" >> $GITHUB_PATH

      - name: 设置 ccache 缓存
        uses: actions/cache@v4
        with:
          path: /home/runner/.ccache
          key: kernel-ccache-${{ github.sha }}
          restore-keys: kernel-ccache-

      - name: 源码同步
        run: |
          mkdir kernel_workspace && cd kernel_workspace
          git config --global user.email "bot@example.com"
          git config --global user.name "KernelBot"
          # 使用你指定的 XML 源码清单
          repo init -u https://github.com/SHuiTU68/OnePlus_KernelSU_SUSFS.git -b main -m manifests/oos16/oneplus_ace5_ultra_w.xml
          repo sync -c -j$(nproc) --force-sync --no-clone-bundle --no-tags

      - name: 第一步：集成 KSUN 与深度性能优化 (LTO+FDO)
        run: |
          # 放弃 find 查找，直接定位到 repo 同步后的内核 common 目录
          # 根据你的目录结构，内核通常在 kernel 目录下
          cd kernel_workspace/kernel || cd kernel_workspace
          
          # 1. 集成 pershoot-KSUN
          git clone https://github.com/pershoot/KernelSU-Next ksun_src
          mkdir -p fs/ksun
          cp -r ksun_src/kernel/* fs/ksun/
          echo "obj-y += ksun/" >> fs/Makefile
          
          # 2. 混合钩子适配：禁用 KPROBES
          # 使用通配符定位所有 configs，防止路径偏移
          find . -name "*defconfig*" -exec sed -i 's/CONFIG_KPROBES=y/CONFIG_KPROBES=n/g' {} +
          
          # 3. 注入 LTO + FDO 优化参数
          GKI_CONF=$(find . -name "gki_defconfig" | head -n 1)
          if [ -n "$GKI_CONF" ]; then
            echo "CONFIG_LTO_CLANG_FULL=y" >> "$GKI_CONF"
            echo "CONFIG_PGO_CLANG=y" >> "$GKI_CONF"
            echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y" >> "$GKI_CONF"
          fi

      - name: 第二步：执行编译 (全路径锁定)
        run: |
          # 寻找编译脚本并直接执行
          BUILD_SCRIPT=$(find kernel_workspace -name "build_kernel.sh" | head -n 1)
          cd $(dirname "$BUILD_SCRIPT")
          chmod +x build_kernel.sh
          
          export USE_CCACHE=1
          export CCACHE_DIR=/home/runner/.ccache
          ccache -M 10G
          
          # 针对一加 Ace 5 Ultra 平台编译
          ./build_kernel.sh plc110 -j$(nproc)

      - name: 第三步：AnyKernel3 打包
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git AK3
          find kernel_workspace -name "Image" -exec cp {} AK3/ \;
          
          cat <<EOF > AK3/anykernel.sh
          ### AnyKernel3 Mod Script
          properties() {
          kernel.string=PLC110-LTO-FDO-Optimized
          do.devicecheck=0
          do.modules=0
          do.cleanup=1
          }
          block=boot;
          is_slot_device=auto;
          . tools/ak3-core.sh;
          split_boot;
          flash_boot;
          EOF
          
          cd AK3 && zip -r ../PLC110-LTO-FDO-$(date +%Y%m%d).zip *

      - name: 上传刷机包
        uses: actions/upload-artifact@v4
        with:
          name: PLC110-Optimized-Kernel
          path: "*.zip"

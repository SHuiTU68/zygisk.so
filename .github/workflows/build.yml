name: OnePlus MT6991 Fengchi Hybrid SuSFS Final_Fixed
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 环境准备与依赖兼容
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison git libssl-dev libelf-dev zip libncurses-dev
          # 解决旧版 Clang 对 libtinfo5 的依赖
          sudo ln -s /usr/lib/x86_64-linux-gnu/libtinfo.so.6 /usr/lib/x86_64-linux-gnu/libtinfo.so.5 || true

      - name: 2. 获取 Clang r522817 (使用 Git 避开 503)
        run: |
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 clang-tool
          mkdir -p clang
          cp -r clang-tool/clang-r522817/* clang/

      - name: 3. 拉取内核源码与 SuSFS 上游 (全深度克隆)
        run: |
          git clone --depth=1 https://github.com/OnePlusOSS/android_kernel_oneplus_mt6991.git kernel-source
          # SuSFS 仓库不使用 --depth=1，确保文件完整
          git clone https://gitlab.com/simonpunk/susfs4ksu.git susfs-src

      - name: 4. 集成 KSU Next 与 Hybrid 强制魔改
        run: |
          cd kernel-source
          curl -LSs "https://raw.githubusercontent.com/rifsxd/KernelSU-Next/next/kernel/setup.sh" | bash -s next
          
          # [魔改] 按照截图方案删除判断语句，强制 Hybrid
          # 强制编译 hooks.o
          sed -i 's/obj-$(CONFIG_KPROBES) += hooks.o/obj-y += hooks.o/g' kernel/Makefile || true
          # 移除 KPROBES 注册状态检查
          sed -i '/if (!ksu_kprobes_enrolled)/d' kernel/ksu/supercalls.c || true

      - name: 5. 修复路径并集成 SuSFS
        run: |
          cd kernel-source
          # 动态查找 SuSFS 文件路径，防止上游目录更名
          SUSFS_ROOT=$(find ../susfs-src -name "kernel_toolkit" -type d | head -n 1)
          
          if [ -z "$SUSFS_ROOT" ]; then
            echo "错误：找不到 SuSFS 核心目录" && exit 1
          fi

          # 复制核心文件
          cp $SUSFS_ROOT/fs/susfs.c fs/
          cp $SUSFS_ROOT/include/linux/susfs.h include/linux/
          
          # 应用 6.6 内核补丁
          # 优先使用 ksu_next 专用补丁
          PATCH_FILE=$(find $SUSFS_ROOT -name "ksu_next_susfs.patch" | head -n 1)
          patch -p1 < "$PATCH_FILE" || echo "SuSFS 补丁应用略有偏移，请留意"

      - name: 6. 注入风驰 (Fengchi) 补丁与 O2 优化
        # 针对 1000022734.jpg 逻辑的延续
        run: |
          cd kernel-source
          wget https://raw.githubusercontent.com/cctv18/oppo_oplus_realme_sm8750/main/other_patch/fengchi.patch
          patch -p1 < fengchi.patch || echo "风驰补丁已应用"
          
          # 强制 O2 性能优化
          sed -i 's/-Os/-O2/g' Makefile

      - name: 7. 开启内核宏定义
        run: |
          cd kernel-source
          DEFCONFIG="arch/arm64/configs/gki_defconfig"
          scripts/config --file $DEFCONFIG -e CONFIG_KSU_SUSFS
          scripts/config --file $DEFCONFIG -e CONFIG_OPLUS_FEATURE_FENGCHI
          scripts/config --file $DEFCONFIG -e KSU
          scripts/config --file $DEFCONFIG -e KSU_NEXT

      - name: 8. 执行正式编译
        run: |
          cd kernel-source
          export PATH=$GITHUB_WORKSPACE/clang/bin:$PATH
          MAKE_ARGS="ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_COMPAT=arm-linux-gnueabi- CLANG_TRIPLE=aarch64-linux-gnu-"
          
          make -j$(nproc) $MAKE_ARGS gki_defconfig
          make -j$(nproc) $MAKE_ARGS Image.gz

      - name: 9. 打包 AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git AK3
          cp kernel-source/arch/arm64/boot/Image.gz AK3/
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AK3/anykernel.sh
          sed -i 's/block=auto/block=boot/g' AK3/anykernel.sh
          cd AK3 && zip -r9 ../OnePlus-MT6991-Fengchi-SuSFS.zip *

      - name: 10. 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: Fengchi-Hybrid-SuSFS-Fixed
          path: ./OnePlus-MT6991-Fengchi-SuSFS.zip
